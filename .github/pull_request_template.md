## Summary

<!-- Describe what this PR changes and why. -->

## Test plan

<!-- How did you verify this change works? -->

## CHANGELOG

<!-- If this change is user-visible (new feature, behavior change, bug fix, security fix):
     add an entry under `## Unreleased` in `plugins/kusari/CHANGELOG.md`.
     See the "CHANGELOG authoring conventions" section in docs/RELEASING.md.

     If the change requires user action (new prerequisite, removed feature, behavior change),
     lead the entry with **Requires action:** so consumers can identify it from release notes. -->

- [ ] CHANGELOG updated (or not applicable — this change is internal-only)
